{
  "name": "pi-transcribe",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Speech-to-text dictation extension for pi coding agent using parakeet-mlx",
  "scripts": {
    "clean": "echo 'nothing to clean'",
    "build": "echo 'nothing to build'",
    "check": "echo 'nothing to check'"
  },
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "dependencies": {
    "@picovoice/pvrecorder-node": "^1.2.8"
  },
  "devDependencies": {
    "@mariozechner/pi-coding-agent": "*"
  }
}
